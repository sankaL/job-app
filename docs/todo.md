# TODO

- [ ] Allow applications to be deleted from the applications table
- [ ] Replace "Applied" checkbox with a button at end of table to toggle applied status
- [ ] Align filters on applications table with the search bar
- [ ] Add multi-select for delete and multi-mark-as-applied in applications table
- [ ] Add breadcrumb ("Home") to dashboard page
- [ ] Only create draft/application if URL is provided; don't create if closed with no input
- [ ] Add confirmation modal when marking application as applied
- [ ] Make resume markdown editing mode look prettier (add styling)
- [ ] Update login page to reflect current UI state
- [ ] Make base resume editor markdown form look prettier
- [ ] Add toast notifications for: application creation, deletion, and generation completion
- [ ] Remove toast for generation completion since it's already shown on page
- [ ] Various UI improvements in application detail page